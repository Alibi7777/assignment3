<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reviews</title>
  </head>
  <body>
    <h1>Product Reviews</h1>
    <% if (error) { %>
    <p style="color: red"><%= error %></p>
    <% } else { %>
    <h3><%= reviews.title %></h3>
    <p>Price: $<%= reviews.price %></p>
    <p>Description: <%= reviews.description %></p>
    <p>Rating: <%= reviews.rating %> (<%= reviews.reviewCount %> reviews)</p>
    <% } %>
  </body>
</html>
